# Data

Run the data generator from the project root:

```bash
python src/generate_sample_data.py
```

It creates:

- `sample_customers.csv`
- `sample_orders.csv`

The generated data is synthetic and safe to publish. It is designed to mimic common
retail analytics fields such as customer region, signup channel, order date,
category, quantity, unit price, discount, and paid amount.

